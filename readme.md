## How to run

1. Open VS Code
2. Navigate to your current project directory in terminal
3. Run command `npm install` in terminal
4. To Run the Test Cases, Use command `npm test` in terminal

## What all have been implemented?

1. Nth term of Fibonacci Sequence will be returned, once a input is provided.
2. Unit Test is done via Jest.
3. To go in more depth `jest --coverage` is used which generate a detailed report the test cases you done, your test case code coverage, and files included, a html file is also generated which show tabular analysis.

Hope you like the work : )